[pytest]
pythonpath = .
markers =
    unit: tests that do not use network, PostgreSQL, Redis, or AWS
    integration: tests that use local ephemeral infrastructure only
    e2e: tests that may target deployed environments
