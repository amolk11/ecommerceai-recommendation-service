services:

  api:
    build: .
    container_name: recommendation-api

    ports:
      - "8000:8000"

    env_file:
      - .env.docker

    depends_on:
      - redis

  redis:
    image: redis:7-alpine

    container_name: recommendation-redis

    ports:
      - "6379:6379"